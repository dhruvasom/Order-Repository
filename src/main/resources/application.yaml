spring:
  application:
    name: OrderService
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
server:
  port: 8083
inventoryService:
  url: http://localhost:8082/api/v1/inventory-manager